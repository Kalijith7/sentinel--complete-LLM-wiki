# ── Core ─────────────────────────────────────────────────────
httpx>=0.27
pyyaml>=6.0
watchfiles>=0.21
jinja2>=3.1
anyio>=4.0

# ── Document parsing ─────────────────────────────────────────
# Primary: Docling (structure-aware, preserves tables & sections)
docling>=2.5.0

# Fallbacks (used if Docling fails for a file type)
pymupdf>=1.24          # fast PDF text extraction
pypdf>=4.0             # pure-python PDF fallback
python-docx>=1.1       # .docx / .doc
pytesseract>=0.3.10    # image OCR
Pillow>=10.0           # image handling

# ── Output rendering ─────────────────────────────────────────
python-frontmatter>=1.1
markdown>=3.6
matplotlib>=3.8        # charts (Matplotlib as per diagram)

# ── Optional: LangChain for advanced graph walking ───────────
# langchain>=0.2
# langchain-community>=0.2

# ── Optional: FastAPI server ─────────────────────────────────
# fastapi>=0.110
# uvicorn[standard]>=0.29
